'use strict';

// Register `phoneList` component, along with its associated controller and template
angular.
  module('omnifoodApp').
  component('contactForms', {
    templateUrl: 'app/contact-forms/contact-forms.template.html',
    controller: function ContactFormsController() {
    }
  });