'use strict';

// Register `phoneList` component, along with its associated controller and template
angular.
  module('omnifoodApp').
  component('sectionCities', {
    templateUrl: 'app/section-cities/section-cities.template.html',
    controller: function SectionCitiesController() {
    }
  });