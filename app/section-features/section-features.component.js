'use strict';

// Register `phoneList` component, along with its associated controller and template
angular.
  module('omnifoodApp').
  component('sectionFeatures', {
    templateUrl: 'app/section-features/section-features.template.html',
    controller: function SectionFeaturesController() {
    }
  });