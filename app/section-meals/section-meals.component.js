'use strict';

// Register `phoneList` component, along with its associated controller and template
angular.
  module('omnifoodApp').
  component('sectionMeals', {
    templateUrl: 'app/section-meals/section-meals.template.html',
    controller: function SectionMealsController() {
    }
  });