'use strict';

// Register `phoneList` component, along with its associated controller and template
angular.
  module('omnifoodApp').
  component('sectionSteps', {
    templateUrl: 'app/section-steps/section-steps.template.html',
    controller: function SectionStepsController() {
    }
  });