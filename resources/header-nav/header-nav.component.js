'use strict';

// Register `phoneList` component, along with its associated controller and template
angular.
  module('headerNav').
  component('headerNav', {
    templateUrl: 'header-nav/header-nav.template.html',
        
    controller: function HeaderNavController() {
      
    }
  });
